-- 意図的な no-op (不可逆)。
--
-- up 移行はリモートアバターデコレーションの行と user.avatarDecorations の
-- JSON 参照を**物理的に削除**する。削除した行や参照を再現する情報は
-- 移行後の DB に残っていないため、down で復元することはできない。
--
-- フェイクの行や参照を再作成してはいけない (壊れた状態を装うことになる)。
-- 復元するには本番 dump から新規ボリュームをリストアするしかない。
SELECT 1;
